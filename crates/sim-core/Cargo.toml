[package]
name = "sim-core"
version = "0.1.0"
edition = "2021"
description = "Engine-agnostic core: time, orbital mechanics, catalog. MUST NOT depend on Bevy (CI-enforced from WP0)."

[dependencies]
serde = { version = "1", features = ["derive"] }
ron = "0.8"
